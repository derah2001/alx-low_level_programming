#include <stdio.h>

/**
 * main - Entry point
 * puts - Print Str
 * Return: Always 0 (Success)
 */
int main()
{

char Str[] = "\"Programming is like building a multilingual puzzle";

puts(Str);
return (0);
}    
