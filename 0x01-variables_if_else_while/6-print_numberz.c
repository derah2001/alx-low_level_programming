#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Rteurn: Always 0 (Success)
 */
int main(void)
{
int nums = 48;
while (nums <= 57)
putchar(nums);
nums++;
}
putchar('\n');
return (0);
}
