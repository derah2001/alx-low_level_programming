#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Rteurn: Always 0 (Success)
 */
int main(void)
{
int nums = 48;
while (nums <= 57)
char putchar(nums);
nums++;
}
char putchar('\n');
return (0);
}
